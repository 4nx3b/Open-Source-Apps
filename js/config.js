/* ============================================================
   Openhouse — database configuration (Supabase)

   1. Create a free project at https://supabase.com
   2. In the SQL editor, run the setup script from README.md
   3. Project Settings → API → copy the two values below
   ============================================================ */
window.OPENHOUSE_DB = {
  // e.g. 'https://abcdefgh.supabase.co'
  url: 'PASTE_YOUR_SUPABASE_URL_HERE',
  // the long "anon / public" key (NOT the service_role key)
  anonKey: 'PASTE_YOUR_SUPABASE_ANON_KEY_HERE'
};
